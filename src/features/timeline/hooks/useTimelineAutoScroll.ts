// Stub — implemented when drag hooks exist.
// During drag, watches pointer X against container edges and calls setScroll
// to auto-scroll when the pointer is within EDGE_THRESHOLD px of either edge.
export function useTimelineAutoScroll() {
    return {};
}
