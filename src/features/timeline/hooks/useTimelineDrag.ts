// Stub — implemented when timeline track components exist.
// Will handle event move drag + resize, reading zoomRef + scrollLeftRef
// and emitting onMoveEvents / onResizeEvent callbacks.
export function useTimelineDrag() {
    return {};
}
